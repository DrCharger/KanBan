export type FlexProp = {
  justify?: string;
  align?: string;
  row?: string;
  height?: string;
  margin?: string;
  width?: string;
  pos?: string;
};

export type ContainerProp = {
  back?: boolean;
};

export type IssueProp = { back?: boolean; draggableStyle: {} };
